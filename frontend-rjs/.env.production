# Production build — used by `vite build` and deployed via Vercel
# This is the fallback; set the same variable in Vercel dashboard too (it overrides this)
VITE_API_BASE_URL=https://neuro-gen-smart-resource-allocation-production.up.railway.app
